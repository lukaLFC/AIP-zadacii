#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void main()
{
    int n, a;
    double e, x0, x1;

    printf("Uneti vrednosti za n, a, e: ");
    scanf("%d%d%lf", &n, &a, &e);

}
